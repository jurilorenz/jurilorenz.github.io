---
import MainLayout from "../../layouts/MainLayout.astro"
---

<MainLayout
  title="Shopify Onlineshop Design aus München – maßgeschneidert & performant | maxWapp"
  seoTitle="Shopify Onlineshop Erstellung & Redesign – Performance & Conversion Fokus | maxWapp"
  seoDesc="Professionelle Shopify Onlineshops aus München. Design von Grund auf oder Redesign deines bestehenden Shops – schnell, mobiloptimiert & verkaufsstark. Jetzt starten!"
>
  <!-- Hintergrund mit Verlauf -->
  <div
    class="min-h-screen bg-gradient-to-b from-[#090a1a] to-[#290065] text-white text-center flex flex-col items-center justify-center"
  >
    <!-- Titel -->
    <h1
      class="text-center text-4xl md:text-6xl font-bold text-gray-100 drop-shadow-lg mt-12 md:mb-12 md:mt-36"
    >
    Online-Shops

    </h1>



    <!-- Inhalt Abschnitt -->
    <div
      class="w-11/12 md:w-10/12 lg:w-8/12 mx-auto text-lg md:text-xl text-gray-300 leading-relaxed mt-12 flex flex-col-reverse md:flex-row items-center gap-16 md:gap-32"
    >
      <!-- Handybildabschnitt für Desktop links -->
      <div
        class="w-full md:w-5/12 flex justify-center md:justify-end mb-12 md:mb-0 rounded shadow-md drop-shadow-[-35px_0px_35px_rgba(75,0,130,0.6)] phone-container"
      >
        <img
          src="/images/projects/kleines-stadthaus-bestsellers.webp"
          alt="Shopify Screenshot"
          class="w-80 md:w-auto md:max-h-[60vh] object-contain rounded-lg"
        />
      </div>

      <!-- Textabschnitt für Desktop rechts -->
      <div class="w-full md:w-5/12 text-center md:text-left">
        <p class="text-xl md:text-2xl mt-4">
          Dein Shopify-Shop sieht gut aus, aber verkauft nicht? Oder du stehst noch ganz am Anfang? Ich helfe dir, das volle Potenzial deines Onlineshops zu entfalten – mit einem maßgeschneiderten<span class="text-gradient"> Design, das verkauft</span>.
        </p>
        
        <p class="text-xl md:text-2xl mt-8">
          Ob <strong>Redesign eines bestehenden Shops</strong> oder <strong>kompletter Neuaufbau</strong> – ich bringe dein Projekt auf Kurs. Viele fertige Themes wirken überladen oder technisch träge. Ich setze auf <span class="text-gradient">Performance, Klarheit und Conversion</span>.
        </p>
        
        <p class="text-xl md:text-2xl mt-8">
          Denn: Ein schöner Shop reicht nicht – er muss funktionieren. Schlechte Usability, langsame Ladezeiten oder überforderndes Design kosten dich bares Geld. Ich optimiere deinen Shopify-Shop <strong>gezielt für deine Zielgruppe</strong>.
        </p>
      </div>
    </div>

   <!-- Was ich biete Abschnitt mit Karten -->
<h2
class="text-center text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-blue-500 my-24"
>
Was ich biete
</h2>

<div
class="grid grid-cols-1 md:grid-cols-2 gap-12 px-6 md:px-0 max-w-6xl mx-auto"
>
<!-- Karte 1: Shop-Redesign mit Conversion-Fokus -->
<div
  class="relative bg-[#1b1b33] p-8 rounded-xl border border-gray-700 shadow-[0px_0px_35px_rgba(147,112,219,0.6)]"
>
  <div
    class="absolute top-0 left-0 bg-gradient-to-r from-purple-400 to-blue-500 w-full h-2 rounded-t-xl"
  ></div>
  <h4 class="text-3xl font-bold text-white mb-6">Redesign mit Conversion-Fokus</h4>
  <p class="text-lg text-gray-300 leading-relaxed">
    Dein aktueller Shop wirkt nicht professionell oder bringt kaum Verkäufe? Ich gestalte ihn <strong>komplett neu</strong> – mit Fokus auf Vertrauen, Klarheit und <strong>maximale Conversion</strong>.
  </p>
</div>

<!-- Karte 2: Shopify Setup & Startklar-Paket -->
<div
  class="relative bg-[#1b1b33] p-8 rounded-xl border border-gray-700 shadow-[0px_0px_35px_rgba(147,112,219,0.6)]"
>
  <div
    class="absolute top-0 left-0 bg-gradient-to-r from-purple-400 to-blue-500 w-full h-2 rounded-t-xl"
  ></div>
  <h4 class="text-3xl font-bold text-white mb-6">Shopify Setup</h4>
  <p class="text-lg text-gray-300 leading-relaxed">
    Du startest bei Null? Ich übernehme alles – von der Theme-Auswahl bis zur fertigen Shopstruktur. Du bekommst einen <strong>markenstarken Onlineshop</strong> – ready to sell.
  </p>
</div>

<!-- Karte 3: Speed & Technik, die verkaufen -->
<div
  class="relative bg-[#1b1b33] p-8 rounded-xl border border-gray-700 shadow-[0px_0px_35px_rgba(147,112,219,0.6)]"
>
  <div
    class="absolute top-0 left-0 bg-gradient-to-r from-purple-400 to-blue-500 w-full h-2 rounded-t-xl"
  ></div>
  <h4 class="text-3xl font-bold text-white mb-6">Speed & Technik, die verkaufen</h4>
  <p class="text-lg text-gray-300 leading-relaxed">
    Ich optimiere Ladezeiten, Navigation und technische Struktur – für <strong>mehr Vertrauen und bessere Nutzererfahrung</strong>. Denn jeder Sekundenbruchteil entscheidet beim Verkauf.
  </p>
</div>

<!-- Karte 4: Mobile-First & Conversion-Ready -->
<div
  class="relative bg-[#1b1b33] p-8 rounded-xl border border-gray-700 shadow-[0px_0px_35px_rgba(147,112,219,0.6)]"
>
  <div
    class="absolute top-0 left-0 bg-gradient-to-r from-purple-400 to-blue-500 w-full h-2 rounded-t-xl"
  ></div>
  <h4 class="text-3xl font-bold text-white mb-6">Mobile-First</h4>
  <p class="text-lg text-gray-300 leading-relaxed">
    Über 70 % der Nutzer kaufen mobil. Ich entwickle deinen Shop <strong>mobile-first</strong> – perfekt auf Smartphone, Tablet & Desktop. Damit keine Verkäufe verloren gehen.
  </p>
</div>
</div>

    <h2 class="text-center text-3xl md:text-4xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-blue-500 mt-36 mb-12" style="line-height: 1.3;">
      Investiere clever – und spare bares Geld.
    </h2>
    
    <p class="text-xl md:text-2xl text-gray-300 text-center max-w-4xl mx-auto">
      Viele Shops sehen schick aus – aber konvertieren nicht. Ich entwickle <span class="text-gradient font-semibold">Shops mit Strategie</span>, die dein Budget nicht verbrennen, sondern <strong>mehr aus deinem Traffic holen</strong>.
    </p>
    


  <!-- Warum mit mir arbeiten Abschnitt -->
  <h2
    class="text-center text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-blue-500 mt-36 mb-24 mx-2"
    style="line-height: 1.3;"
  >
  Shopify-Shop, der wirklich verkauft.
  </h2>

  <div
    class="w-11/12 md:w-10/12 lg:w-8/12 mx-auto flex flex-col md:flex-row items-center mb-24"
  >
    <!-- Handybildabschnitt für Desktop links -->
    <div
      class="w-full md:w-5/12 flex justify-center md:justify-center mb-12 md:mb-0 rounded shadow-md drop-shadow-[-35px_0px_35px_rgba(75,0,130,0.6)] phone-container-second"
    >
      <img
        src="/images/projects/kleines-stadthaus-kollektionen.webp"
        alt="Shopify Screenshot Kollektion"
        class="w-80 md:w-auto md:max-h-[60vh] object-contain rounded-lg"
      />
    </div>

    <!-- Textabschnitt für Desktop rechts -->
    <div class="w-full md:w-5/12 text-center md:text-left">
      <p class="text-xl md:text-2xl mt-4">
        Viele Shops sehen gut aus, aber verkaufen nicht. Der Unterschied? Strategie.
      </p>
  
      <p class="text-xl md:text-2xl mt-8">
        Ich verbinde <strong>Design, Technik und Marketingwissen</strong> zu einem Shopify-Shop, der wirklich funktioniert – für dich, deine Zielgruppe und <span class="underline-animation">deinen Umsatz</span>.
      </p>
  
      <p class="text-xl md:text-2xl mt-8">
        <span class="text-gradient font-semibold">Conversion, Geschwindigkeit und Klarheit</span> stehen im Fokus – nicht Spielereien. Damit dein Budget arbeitet – statt verpufft.
      </p>

      <!-- Buttons at the bottom-right -->
      <div
        class="flex flex-col md:flex-row gap-6 mt-8 md:mt-16 justify-center md:justify-start items-center md:items-start w-full md:w-auto"
      >
        <!-- Secondary Button -->
        <a
          href="/projekte"
          class="bg-[#1f1235] text-white font-bold py-4 px-10 rounded-full border-2 border-[#6d87d1] shadow-lg transition-all duration-300 ease-in-out hover:bg-blue-500 hover:shadow-[0_0_10px_rgba(147,112,219,0.5),_0_0_25px_rgba(3,169,244,0.4)] text-center w-full max-w-xs md:max-w-none md:w-auto"
        >
          Projekte erleben
        </a>

        <!-- Primary Button -->
        <a
          href="/kontakt"
          class="relative inline-block py-4 px-10 rounded-full font-bold text-center w-full max-w-xs md:max-w-none md:w-auto bg-white text-[#090a1a] shadow-lg transition-all duration-300 ease-in-out hover:shadow-[0_0_20px_rgba(147,112,219,0.8),_0_0_35px_rgba(3,169,244,0.6)] hover:border-[#6d87d1]"
        >
          Let's talk
        </a>
      </div>
    </div>

    <style>
      .text-gradient {
        background: linear-gradient(to right, #9c27b0, #03a9f4);
        -webkit-background-clip: text;
        color: transparent;
      }

      .drop-shadow-lg {
        filter: drop-shadow(0 8px 16px rgba(0, 0, 0, 0.2));
      }

      .border-gradient-to-r {
        border-image-slice: 1;
        border-image-source: linear-gradient(to right, #9c27b0, #03a9f4);
      }

      /* Container for the entire image, gradient, and shadow */
      .profile-container {
        transform: translateX(-150%); /* Start off-screen to the left */
        opacity: 0; /* Initially hidden */
        transition:
          transform 1.5s ease-out,
          opacity 1.5s ease-out;
      }

      /* Trigger the animation when the page loads */
      .profile-container {
        animation: flyInFromLeft 1.5s forwards;
      }

      /* Keyframes for the fly-in effect */
      @keyframes flyInFromLeft {
        0% {
          transform: translateX(-150%);
          opacity: 0;
        }
        100% {
          transform: translateX(0);
          opacity: 1;
        }
      }

      /* Container for the entire image and shadow */
      .phone-container {
        transform: translateX(-150%); /* Start off-screen to the left */
        opacity: 0; /* Initially hidden */
        transition:
          transform 1.5s ease-out,
          opacity 1.5s ease-out; /* Remove delay for mobile */
      }

      /* Fly in effect */
      .fly-in {
        transform: translateX(0);
        opacity: 1;
      }

      /* Keyframes for desktop (initial load) */
      @media (min-width: 768px) {
        .phone-container {
          animation: flyInFromLeftPhone 1.5s forwards 0.5s; /* Keep delay on desktop */
        }

        .underline-animation {
          display: inline-block;
          background-image: linear-gradient(120deg, #03a9f4, #9c27b0);
          background-repeat: no-repeat;
          background-size: 100% 3px;
          background-position: 0 88%;
          transition: background-size 0.3s ease-in-out;
        }

        .underline-animation:hover {
          background-size: 100% 100%;
        }
      }

      /* Keyframes for mobile (on scroll) */
      @keyframes flyInFromLeftPhone {
        0% {
          transform: translateX(-150%);
          opacity: 0;
        }
        100% {
          transform: translateX(0);
          opacity: 1;
        }
      }

      /* Initial state for the second phone container */
      .phone-container-second {
        transform: translateX(-150%); /* Positioned off-screen to the left */
        opacity: 0; /* Initially hidden */
        transition:
          transform 1.5s ease-out,
          opacity 1.5s ease-out;
      }

      /* Fly-in effect when in view */
      .fly-in-second {
        transform: translateX(0); /* Return to original position */
        opacity: 1; /* Fully visible */
      }
    </style>

    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const phoneImage = document.querySelector(".phone-container")

        // Detect if the element is in view for mobile
        function isInViewPort(element) {
          const rect = element.getBoundingClientRect()
          return (
            rect.top <=
              (window.innerHeight || document.documentElement.clientHeight) &&
            rect.bottom >= 0
          )
        }

        // Add event listener for scroll to trigger the animation
        window.addEventListener("scroll", function () {
          if (window.innerWidth < 768) {
            // Only apply for mobile view
            if (isInViewPort(phoneImage)) {
              phoneImage.classList.add("fly-in")
            }
          }
        })
      })

      document.addEventListener("DOMContentLoaded", function () {
        const secondPhoneImage = document.querySelector(
          ".phone-container-second"
        )

        // Function to check if the element is in the viewport
        function isInViewPort(element) {
          const rect = element.getBoundingClientRect()
          return (
            rect.top <=
              (window.innerHeight || document.documentElement.clientHeight) &&
            rect.bottom >= 0
          )
        }

        // Function to add the fly-in class when in view
        function handleScroll() {
          if (isInViewPort(secondPhoneImage)) {
            secondPhoneImage.classList.add("fly-in-second")
          }
        }

        // Add scroll event listener
        window.addEventListener("scroll", handleScroll)

        // Run on page load in case the element is already in view
        handleScroll()
      })
    </script>
  </div>
</MainLayout>
