---
// DevicesComponent.astro
---

<div class="relative w-full h-full flex items-center shadow-md drop-shadow-[-35px_0px_35px_rgba(75,0,130,0.6)] justify-center py-48 md:py-96 overflow-hidden">
  
  <!-- iPad Image -->
  <img src="/images/projects/6.webp" 
       class="iphone-image absolute bottom-0 object-contain max-h-full"
       alt="iPhone Image" />

<style>
/* iPhone Animation and Proportions */
.iphone-image {
  animation: flyInFromRight 1.8s ease-out forwards;
  position: absolute;
  bottom: 0;
  max-height: 100%; /* Ensures the iPad height stays within the parent container */
  width: auto;
  object-fit: contain; /* Ensures the aspect ratio is maintained */
  transform: rotateX(0deg);
}

@keyframes flyInFromRight {
  0% {
    transform: translateX(100%);
    opacity: 0;
  }
  100% {
    transform: translateX(0);
    opacity: 1;
  }
}

/* Limit the height for the component itself */
@media (min-width: 7px) {
  .relative {
    max-height: 80vh; /* Limit the overall height of the component */
  }
}

@media (max-width: 7px) {
  .ipad-image {
    width: 90%;
    bottom: 10%;
  }
}
</style>

