---
const { title, description, url, image, alt, highlight } = Astro.props;
---

<a href={url} class="relative bg-[#1b1b33] p-8 rounded-xl border border-gray-700 shadow-[0px_0px_35px_rgba(147,112,219,0.6)] hover:shadow-2xl transition-transform duration-500 hover:scale-105 flex flex-col items-center">
  <div class="absolute top-0 left-0 bg-gradient-to-r from-purple-400 to-blue-500 w-full h-2 rounded-t-xl"></div>
  <img class="rounded-lg shadow-md drop-shadow-[0px_-35px_35px_rgba(75,0,130,0.6)] object-contain w-full h-auto max-h-[60vh]" src={image} alt={alt} />
  <h3 class="font-bold mb-4 mt-8 text-xl text-white">{title}</h3>
  <p class="text-gray-300 leading-relaxed">
    {description}
    {highlight && (
      <span class="bg-gradient-to-r from-purple-400 to-blue-500 bg-clip-text text-transparent font-bold text-lg">
        {highlight}
      </span>
    )}
  </p>
</a>