---
// FinalSection.astro

export interface FinalSectionProps {
  imgSrc: string
  imgAlt: string
  title: string
  introText: string
  secondText: string
  secondaryButtonLabel: string
  secondaryButtonLink: string
}

const {
  imgSrc,
  imgAlt,
  title,
  introText,
  secondText,
  secondaryButtonLabel,
  secondaryButtonLink,
} = Astro.props
---

<div
  class="w-full md:w-10/12 lg:w-full mx-auto mt-20 mb-32 flex flex-col md:flex-row items-center gap-16 md:gap-32"
>
  <!-- Image -->
  <div class="w-full md:w-6/12 lg:w-5/12 flex justify-center md:justify-end">
    <img
      src={imgSrc}
      alt={imgAlt}
      class="w-full max-w-[450px] rounded-xl shadow-lg object-contain"
    />
  </div>

  <!-- Text + CTA -->
  <div class="w-full md:w-6/12 lg:w-7/12 text-center md:text-left">
    <h2
      class="mb-10 text-4xl md:text-5xl font-extrabold leading-tight md:leading-normal"
    >
      {title}
    </h2>

    <p class="text-xl md:text-2xl mt-4">{introText}</p>
    <p class="text-xl md:text-2xl mt-4">{secondText}</p>

    <!-- Buttons with hover effects -->
    <div
      class="flex mt-8 md:mt-16 justify-center md:justify-start w-full md:w-auto"
    >
      <!-- CTA Button -->
      <a
        href={secondaryButtonLink}
        class="py-4 px-10 rounded-full font-bold text-center w-full max-w-xs md:max-w-none md:w-auto bg-white text-[#090a1a] shadow-lg transition-all duration-300 ease-in-out hover:shadow-[0_0_20px_rgba(147,112,219,0.8),_0_0_35px_rgba(3,169,244,0.6)]"
      >
        {secondaryButtonLabel}
      </a>
    </div>
  </div>
</div>
